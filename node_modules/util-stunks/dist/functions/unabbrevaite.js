"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const numbers_json_1 = __importDefault(require("../info/numbers.json"));
const unabbreviate = (input) => {
    if (typeof input !== 'string')
        throw new TypeError('Parâmetro deve ser do tipo string');
    const num = parseFloat(input);
    const unit = input.substr(-1).toLowerCase();
    const abbr = Object.keys(numbers_json_1.default).map((x) => x.toLowerCase());
    if (abbr.indexOf(unit) === -1)
        return parseFloat(input);
    return num * Object.values(numbers_json_1.default)[abbr.indexOf(unit)];
};
exports.default = unabbreviate;
